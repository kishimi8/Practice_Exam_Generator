name: Build and Release Desktop App

permissions:
  contents: write

on:
  push:
    branches:
      - main
    tags:
      - 'v*'
      - 'V*'
  workflow_dispatch:

jobs:
  build-macos:
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install pyinstaller pyinstaller-hooks-contrib PySide6

      - name: Build macOS app
        run: |
          python -m PyInstaller --noconfirm build.spec

      - name: Zip macOS dist
        run: |
          cd dist
          zip -r ../practice-exam-generator-macos.zip .

      - name: Upload macOS artifact
        uses: actions/upload-artifact@v4
        with:
          name: practice-exam-generator-macos
          path: practice-exam-generator-macos.zip

  build-windows:
    runs-on: windows-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          python -m pip install pyinstaller pyinstaller-hooks-contrib PySide6

      - name: Build Windows app
        run: |
          python -m PyInstaller --noconfirm build.spec

      - name: Zip Windows dist
        shell: powershell
        run: |
          Compress-Archive -Path 'dist\*' -DestinationPath 'practice-exam-generator-windows.zip' -Force

      - name: Upload Windows artifact
        uses: actions/upload-artifact@v4
        with:
          name: practice-exam-generator-windows
          path: practice-exam-generator-windows.zip

  release:
    needs: [build-macos, build-windows]
    runs-on: ubuntu-latest
    if: >-
      ${{
        needs.build-macos.result == 'success' &&
        needs.build-windows.result == 'success' &&
        (
          github.event_name == 'workflow_dispatch' ||
          github.ref == 'refs/heads/main' ||
          startsWith(github.ref, 'refs/tags/v')
        )
      }}

    steps:
      - name: Download macOS artifact
        uses: actions/download-artifact@v4
        with:
          name: practice-exam-generator-macos
          path: release-artifacts

      - name: Download Windows artifact
        uses: actions/download-artifact@v4
        with:
          name: practice-exam-generator-windows
          path: release-artifacts

      - name: Create release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ github.ref_name }}
          name: Release ${{ github.ref_name }}
          files: |
            release-artifacts/practice-exam-generator-macos.zip
            release-artifacts/practice-exam-generator-windows.zip
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
